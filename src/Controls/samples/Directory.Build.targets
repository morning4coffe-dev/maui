<Project>
  <Import Project="$(MauiSrcDirectory)Maui.InTree.targets" Condition=" '$(UseMaui)' != 'true' and '$(UnoMauiUseSource)' != 'false' " />
  <Import Project="$(MauiRootDirectory)Directory.Build.targets" />
</Project>
